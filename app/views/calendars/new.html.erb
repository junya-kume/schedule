<%= render "header" %>

<div class="contents row">
  <div class="container">
    <h3>予定を入れる</h3>
    <%= form_with(model: @calendar, local: true) do |form| %>
      <div class="">
        タイトル<br>
        <%= form.text_field :title, class: "title-text", placeholder: "Title" %>
      </div>
      <div class="">
        日付<br>
        <%= form.datetime_select :start_time, {use_month_numbers: true, start_year: Date.today.year, datetime_separator: '<div class="fb-txt font-weight-bold my-2">時間</div>'},{class: "form-control", id: 'ymd-date'} %>
      </div>
      <div class="">
        内容<br>
        <%= form.text_area :content, class: "title-text", placeholder: "Content", rows:"7", maxlength:"1000" %>
      </div>
      <%= form.submit "SEND",value: "送信" %>
    <% end %>
    <%= link_to '戻る', calendars_path %>
  </div>
</div>